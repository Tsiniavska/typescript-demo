// * Base
import { memo } from 'react';

// * Components
import CommentsList from '../../../components/CommentsList/CommentsList';

const ListComments = memo(() => <CommentsList />);

export default ListComments;
