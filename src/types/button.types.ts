export enum EButton {
  BUTTON = 'button',
  SUBMIT = 'submit',
  RESET = 'reset',
}

export enum EDesign {
  DEFAULT = 'default',
  WHITE = 'white',
  BIG = 'big',
  BIGWHITE = 'bigWhite',
  RETRY = 'retry',
}
